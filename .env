# Public, non-sensitive env defaults. Real values go in .env.local (gitignored).

# Supabase (overridden by .env.local for the active project)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Stripe (boilerplate placeholders — unused until billing is wired)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
BILLING_PLAN_ENV=dev
